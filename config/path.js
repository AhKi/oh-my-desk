const path = require('path');

export const ROOT_PATH = path.join(__dirname, '..');
export const APP_PATH = path.join(ROOT_PATH, 'app');
export const BUILD_PATH = path.join(ROOT_PATH, 'build');
export const CONFIG_PATH = path.join(ROOT_PATH, 'config');
export const TEST_PATH = path.join(ROOT_PATH, 'test');
